# 파일 생성
vi .bashrc 명령어를 통해 파일을 생성하면 다음과 같이 파일 생성
    # .bashrc
    
    # Source global definitions
    if [ -f /etc/bashrc ]; then
            . /etc/bashrc
    fi
    
    # Uncomment the following line if you don't like systemctl's auto-paging feature:
    # export SYSTEMD_PAGER=

    # User specific aliases and functions

    # 아래에 별칭 기입
    # [예시]
    alias q='cd .ssh && ssh -i uat.pem ec2-user@172.31.61.152'
    unalias [alias.name]

# alias 적용
source .bashrc 명령어를 통해 기입한 별칭을 적용